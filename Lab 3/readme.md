In this lab I set up an Ml environment and load winedataset get the shape and head of the set and overview then clean up the data through EDA. The Data is split then impletment into two diffrent model logistic and decison tree both are reviewed for accuracy withh logistic being more accurate. Visual figures are made using confusion matrix. Other models are reviewed challenges were making sure all software in installed correctly. The reason for this lab was to Understand different ML types and use EDA